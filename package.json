{
  "name": "momentic-examples",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "^22.12.0 || >=24.0.0"
  },
  "scripts": {
    "lint": "npm run lint:web && npm run lint:mobile && npm run lint:workspace",
    "lint:web": "momentic lint --config web/momentic.config.yaml",
    "lint:mobile": "momentic-mobile lint --config android/momentic.config.yaml && momentic-mobile lint --config ios/momentic.config.yaml",
    "lint:workspace": "momentic lint --config multi-project-workspace/apps/dashboard/momentic.config.yaml && momentic lint --config multi-project-workspace/apps/marketing/momentic.config.yaml && momentic lint --config multi-project-workspace/qa/momentic.config.yaml"
  },
  "devDependencies": {
    "momentic": "3.59.3",
    "momentic-mobile": "1.33.2",
    "qa": "0.19.0"
  },
  "packageManager": "npm@10.9.2"
}
